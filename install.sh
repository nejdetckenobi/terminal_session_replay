#!/usr/bin/env bash

wget -O "$HOME/bin/session_play" "https://raw.githubusercontent.com/nejdetckenobi/terminal_session_replay/master/session_play"
wget -O "$HOME/bin/session_record" "https://raw.githubusercontent.com/nejdetckenobi/terminal_session_replay/master/session_record"
chmod +x "$HOME/bin/session_play"
chmod +x "$HOME/bin/session_record"

